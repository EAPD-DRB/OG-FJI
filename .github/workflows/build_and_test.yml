name: Build and test
on:
  push:
    paths:
      - '**.yml'
      - '**.toml'
      - '**.py'
      - '**.json'
      - '**.csv'
      - '**.pkl'
      - '**.pbz2'
      - '**.lock'
  pull_request:
    paths:
      - '**.yml'
      - '**.toml'
      - '**.py'
      - '**.json'
      - '**.csv'
      - '**.pkl'
      - '**.pbz2'
      - '**.lock'
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.12", "3.13"]

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false
      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package and dependencies
        run: uv sync --extra dev
      - name: Test
        run: uv run python -m pytest -m "not local" --cov=./ --cov-report=xml
      # Coverage upload is advisory until this repo has a CODECOV_TOKEN secret.
      # Without one, Codecov rejects the upload ("Token required because branch
      # is protected") and a true `fail_ci_if_error` turns an otherwise passing
      # build red. Add the secret in repository settings, then set
      # `fail_ci_if_error: true` to match the sibling country repos.
      - name: Upload coverage to Codecov
        if: matrix.os == 'ubuntu-latest' && contains(github.repository, 'EAPD-DRB/OG-FJI') && github.actor != 'dependabot[bot]'
        uses: codecov/codecov-action@v7
        with:
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
          verbose: true
  pip-import-smoke:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package and pytest
        run: |
          python -m pip install --upgrade pip
          python -m pip install . pytest

      - name: Run import smoke tests outside checkout
        shell: bash
        run: |
          tmpdir="$(mktemp -d)"
          cp "${{ github.workspace }}/tests/test_import_smoke.py" "$tmpdir/test_import_smoke.py"
          cd "$tmpdir"
          python -m pytest "$tmpdir/test_import_smoke.py"
